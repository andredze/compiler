#include "encoding_nasm.h"

//==========================================================================================

BackendErr_t AssembleInstruction(BackendCtx_t* backend_ctx, Instruction_t* instr)
{
    assert(backend_ctx);
    assert(instr);

    

    return BACKEND_SUCCESS;
}

//==========================================================================================
